# Minimal Bootstrap Skill

This fixture exercises the Story 1.1 passthrough path: no directives,
no variables, no includes — just plain Markdown that should compile
to byte-identical output.

## Usage

```bash
bmad compile --skill <path> --install-dir <out>
```

The compiler writes `SKILL.md` under the install directory.
